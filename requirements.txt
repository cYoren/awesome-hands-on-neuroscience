# Awesome Hands-on Neuroscience - Python Dependencies
# Install with: pip install -r requirements.txt
#
# System dependencies required for some notebooks:
#   - Notebooks 09 (FSL), 10 (AFNI), 11 (MRtrix3): install those CLI tools from their official sites
#   - Notebook 14 (BIDS/datalad): requires git-annex (apt install git-annex / brew install git-annex)
#
# Heavy packages (notebooks 12 & 13 only):
#   torch, torch-geometric, and torch-scatter are ~1 GB combined.
#   Comment them out if you only need the other notebooks.

# Core scientific stack
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pandas>=2.0
scikit-learn>=1.3
seaborn>=0.12

# Jupyter
jupyterlab>=4.0
ipywidgets>=8.0

# Neuroimaging
nibabel>=5.0
nilearn>=0.10
pybids>=0.16
neuromaps>=0.0.5

# MEG/EEG
mne>=1.6

# Computational neuroscience
brian2>=2.7
neurolib>=0.6
neuron>=8.2          # required by netpyne; pip-installable on Linux/macOS
netpyne>=1.0

# Deep learning (notebooks 12 & 13)
torch>=2.0
torch-geometric>=2.4

# Graph analysis
networkx>=3.0

# Image registration
antspyx>=0.4

# Data management
datalad>=0.19        # also needs git-annex from your system package manager

# Utilities
tqdm>=4.65
requests>=2.28
h5py>=3.8
